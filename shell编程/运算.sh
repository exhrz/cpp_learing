#!/bin/bash
# 基本运算符
# 基本语法
# $((运算式)) 或 $[运算式]
# expr m + n 注意 expr 运算符间要有空格
# expr m - n
# expr \*，/，% 分别代表乘，除，取余

echo $(((2 + 3)*4))  # 正确，输出 20
echo $[(2 + 3)*4]    # 正确，输出 20（兼容旧版本Shell）

# 使用expr的正确写法
# 使用赋值的时候，"="两边不能有空格
Temp=$(expr 2 + 3)   # 正确赋值，等号两边无空格
echo $(expr $Temp \* 4)  # 输出 20
